import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

/**
 * Vite configuration for a React + TypeScript frontend application
 *  **/ 
export default defineConfig({
  plugins: [react()],
});
